<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FinalProj" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:scrollbarThumbHorizontal">@color/cream</item>
        <item name="android:scrollbarTrackHorizontal">@color/cream</item>
        <item name="android:scrollbarThumbVertical">@color/cream</item>
        <item name="android:scrollbarTrackVertical">@color/cream</item>
        <item name="android:selectedWeekBackgroundColor">@color/red</item>
    </style>

    <style name="Theme.FinalProj" parent="Base.Theme.FinalProj" />
</resources>